name: unit-tests
on: [push]
jobs:
  test:
    name: Unit-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Java
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '11'
      - name: Run unit-tests
        run: mvn clean test